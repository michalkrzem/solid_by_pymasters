[tool.poetry]
name = "solid"
version = "0.1.0"
description = ""
authors = ["mkrzeminski <mkrzeminski@imgw.pl>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
pytest = "^8.1.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
